# Teaching Session HTTP smoke (2026-09-12)

The main API was started with an operator Workbench configuration and the
smoke Workbench backend. Through the same-origin teaching API, the check:

1. created a Guided L1 session;
2. answered its clarification question;
3. started the L1 Runtime baseline asynchronously; and
4. polled the session snapshot until the state became `observed`.

The terminal Runtime status was `succeeded`. No A2 claim is made by this smoke;
real A2 configuration requires the admitted ORFS/A2 assets and its native
budget.
